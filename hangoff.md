# 天然橡胶产区天气面板交接记录

更新日期：2026-09-13（Asia/Shanghai）

## 一、当前结论

- 【事实】面板功能代码已进入远端 `main`，当前远端提交为 `42301f7`。
- 【事实】每周天气文字总结、未来7天每6小时降雨预报、泰国分区周度降雨历史及东南亚最近四周气温距平展示/归档逻辑已加入面板。
- 【事实】GitHub Actions 手动运行 #17 失败；天气抓取、依赖安装和测试均成功，失败发生在“保存本次更新”，因此 Pages 打包和发布被跳过。
- 【事实】公开 Actions 接口只能确认退出码为1，未返回该步骤的具体 stderr；当前无法确认是 `git commit`、`git pull/push`还是权限问题。
- 【本项目判断】面板功能基本完成，当前最高优先级不是继续加指标，而是先修复自动保存与发布链路。

## 二、已经完成的任务

### 1. 基础面板

- 覆盖中国、泰国、印度尼西亚、越南、科特迪瓦，共29个代表性网格点。
- 支持国家、天气状态和地点搜索筛选。
- 展示7日累计降雨、雨日、强降雨日、最高/最低温、土壤水分和天气状态。
- 点击地图点或表格行可查看逐日明细。
- 已移除网页端的“重新读取”“更新数据”等维护操作，避免普通访客误操作。

### 2. 地理展示

- 已加入亚洲和西非产区地图。
- 底图使用 Natural Earth 1:110m 公共领域行政边界。
- 地图点为研究定位点，不代表种植面积边界或气象站位置。

### 3. 未来天气与割胶作业窗

- Open-Meteo Best Match 提供未来7日模式网格预报。
- “割胶时段降雨”已更名为“晨间割胶作业窗”。
- 统一研究窗口已调整为各地当地时间 `02:00–10:00`。
- 已统计该窗口内的7日降雨、雨日、雨小时和最大降雨概率。
- 该窗口是研究口径，不代表所有产区统一的实际割胶班次。

### 4. IMERG实况与预报兑现率

- 已接入 NASA GPM IMERG Late Run GIS 过去24小时、72小时累计降水。
- GitHub Actions 使用仓库密钥 `NASA_PPS_EMAIL`，密钥值为NASA PPS注册邮箱。
- 已实现基于经纬度的0.1°网格取值和缺失值识别。
- 已实现预报兑现率：`IMERG实况 / 验证期开始前的Open-Meteo预报 × 100`。
- 严格按完整UTC日对齐，并禁止使用验证期开始后的预报，避免前视偏差。
- 预报低于1 mm时不计算兑现率；兑现率是降水量实现比，不是准确率。

### 5. NOAA/CPC东南亚降水图

- 过去7日累计降水。
- 过去7日降水距平。
- 过去15日降水距平。
- 过去30日降水距平。
- 未来1—7日累计降水及距平预报。
- 未来8—14日累计降水及距平预报。

### 6. ENSO与印度洋偶极子

- BoM Relative Niño3.4周度历史图。
- BoM Indian Ocean Dipole周度历史图。
- NOAA CPC官方RONI月度展望图。
- BoM周度文本由更新脚本生成本地SVG；NOAA展望图从官方页面识别并保存到本地。
- 数据源更新失败时保留上次成功图像，不用空值覆盖现有图。
- 已明确提示：BoM相对Niño3.4与NOAA RONI口径不可直接混用；气候状态不等于产区降雨或供应结论。

### 7. 自动化与质量控制

- GitHub Actions 支持手动运行和每周五21:00（Asia/Shanghai）定时运行。
- 天气更新、IMERG取值、历史快照、兑现率、气候图和Pages发布已串入同一工作流。
- 结构化缺失值保持为空，不补0、不沿用前值。
- 当前测试覆盖：天气汇总、状态分类、晨间割胶作业窗、IMERG网格取值、产品日期、无前视兑现率、历史长度、BoM解析与SVG输出、发布控制。

### 8. 每周天气文字总结

- 位于4个概览指标卡下方、地图和明细数据上方，阅读顺序为“数字概览 → 文字研判 → 地图/表格下钻”。
- 由当前 `weather.json` 自动生成，不新增数据源或人工维护文件。
- 覆盖整体概览、未来7日重点降雨区、晨间割胶作业窗、IMERG过去72小时实况和本项目关注。
- 使用全部29个代表点，不随页面筛选条件变化；标题显示统计周期和样本数。
- 事实与本项目判断分层展示，并明确天气信号不等于天然橡胶供应或价格结论。

### 9. 每6小时降雨预报

- 将Open-Meteo小时降雨预报汇总为未来7天、共28个UTC自然6小时时段。
- 表头为时段起点，所有产区使用同一UTC时间轴，便于跨地区横向比较。
- 宽表跟随国家、天气状态和地点筛选；点击行可同步打开该地点日度明细。
- 使用降雨色阶显示0、0.1—4.9、5—14.9、15—29.9和≥30 mm。
- 任一小时缺失时，该6小时累计保持为空，不补0；原始来源仍为Open-Meteo模式网格预报。

### 10. 泰国分区周度降雨历史

- 覆盖2022年至最新可用完整周，按泰国当地时间周一至周日累计。
- 展示泰国重点区域综合、南部东海岸、南部西海岸、东北部和北部，当年与历史各年按周序对比。
- 原始值为Open-Meteo Historical Weather API的ERA5 0.25°再分析日降雨；通常约滞后5天。
- 各分区为代表网格点周累计的等权均值；“泰国重点区域综合”不是全国行政面积加权降雨。
- 只有所有代表点满7天的周才生成数值；缺失周保持为空，不补0。

### 11. 东南亚周度气温距平

- 接入NOAA Climate Prediction Center东南亚周度气温距平图，单位为℃；面板展示最近4个已归档的不同官方周图。
- 产品使用GTS地面站资料生成，属于初步分析；红色表示偏暖，蓝色表示偏冷，空白区不得解释为距平0℃。
- NOAA稳定图片地址只保留当前版本；更新脚本按SHA256去重并滚动保留最近4张，未归档历史明确标记`MISSING`，不重复最新图伪装历史。
- 归档从本功能上线时开始，当前已归档1张；后续再出现3个不同官方周图后自动补齐四周，有效期以图内标题为准。
- 2026-09-13核验时，官方最新图有效期为2026-08-30至09-05，文件最后修改时间为2026-09-09。
- 该图用于识别区域偏暖/偏冷背景，不得单独推导割胶量、产量或价格方向。

## 三、数据源与更新口径

| 模块 | 官方来源 | 类型 | 当前更新方式 | 关键限制 |
|---|---|---|---|---|
| 未来7日天气及6小时降雨 | Open-Meteo | 数值模式预报 | 手动/每周更新 | 不是地面站观测；6小时值按UTC累计 |
| 泰国分区周度降雨 | Open-Meteo / ERA5 | 再分析网格估算 | 每周更新 | 0.25°；约滞后5天；网格点等权而非行政面积加权 |
| 24h/72h降水 | NASA GPM IMERG Late Run GIS | 卫星多源融合估算 | 手动/每周更新 | 不是地面雨量站实测；需PPS凭据 |
| 东南亚降水分析 | NOAA CPC Unified | 雨量站网格分析 | 浏览器读取官方图 | 0.5°网格；距平基准1991—2020 |
| 东南亚周度气温距平 | NOAA Climate Prediction Center | GTS地面站初步分析 | 每周归档官方当前图 | 滚动保留最近4个不同版本；单位℃；有效期见图内标题 |
| 1—14日降水 | NOAA CPC/NCEP GFS集合 | 模式预报 | 浏览器读取官方图 | 经最近30日误差订正 |
| Relative Niño3.4 | Australian Bureau of Meteorology | 周度海温相对指数 | 脚本读取官方文本并生成SVG | 基准1991—2020；不能与RONI混算 |
| Indian Ocean Dipole | Australian Bureau of Meteorology | 周度海温指数 | 脚本读取官方文本并生成SVG | 属气候背景，不直接代表产区降雨 |
| RONI outlook | NOAA Climate Prediction Center | 月度气候展望 | 脚本保存官方当前图 | 是概率/区间展望，不是确定性天气预报 |

## 四、当前未完成事项

### P0：修复GitHub Actions保存与发布

【事实】运行 #17 的步骤状态：

1. 读取仓库：成功。
2. 准备Python：成功。
3. 安装IMERG依赖：成功。
4. 获取最新天气：成功。
5. 校验数据处理逻辑：成功。
6. 保存本次更新：失败。
7. 配置、打包及部署GitHub Pages：跳过。

下一步应在GitHub打开：

`Actions → 运行 #17 → update-and-package → 保存本次更新`

展开日志并记录第一条明确的 `fatal:`、`error:` 或 `rejected` 信息。不要只根据总页的“exit code 1”猜测原因。

常见分支处理：

- 若出现 `rejected`、`fetch first` 或 `non-fast-forward`：在工作流推送前同步远端，避免自动数据提交与人工提交冲突。
- 若出现 `Permission denied` 或 `403`：检查 `Settings → Actions → General → Workflow permissions` 是否允许 Read and write permissions。
- 若出现 `pathspec ... site/assets/climate`：确认气候图片目录已进入 `main`。
- 若出现其他错误：以该步骤的首条明确错误为准，再修改工作流。

在P0修复前，不建议反复点击 `Run workflow`；同一并发组会继续排队，不能解决保存步骤本身的问题。

### P1：补充来源新鲜度状态

- 为 Open-Meteo、IMERG、BoM、NOAA CPC 分别显示“产品日期/最后成功更新时间/质量状态”。
- 设置过期提示：来源超过预期更新周期时标记 `WARNING`，不得静默当作最新数据。
- 气候图更新失败时虽然会保留旧图，但目前网页没有直接显示“旧图正在沿用”。

### P1：积累并检查兑现率样本

- 首次运行或缺少可严格对齐的历史预报时，兑现率显示 `—` 属正常现象。
- 至少积累数周样本后，检查不同地区的覆盖率、偏差方向和异常值。
- 兑现率只能验证降水预报，不得直接转化为割胶损失或供应结论。

### P2：建议新增的高频背景指标

- MJO RMM相位与振幅：日度或周度，适合解释东南亚未来1—3周降雨背景。
- 热带气旋路径与预警：事件驱动，重点覆盖南海、西北太平洋、孟加拉湾。
- 产区地面站降雨交叉验证：只在能稳定取得官方站点数据、且站点代表性已核验后接入。

【本项目判断】优先级为“发布稳定性 → 来源新鲜度 → 兑现率样本质量 → 新指标”。在P0和P1完成前，不建议继续增加大量图表。

## 五、本地Git状态提醒

- 【事实】当前本地活动分支为 `add-production-map`，但它跟踪的是 `origin/main`。
- 【事实】本地 `main` 仍停留在较早提交 `6f221a4`。
- 【本项目判断】后续建议切回本地 `main`，执行 Pull origin 使其快进到远端最新提交，然后以 `main` 作为日常分支；避免继续产生名称混乱的合并提交。
- 切换前必须确认GitHub Desktop显示 `No local changes`。

## 六、恢复工作时的检查顺序

1. 阅读本文件和 `README.md`。
2. 在GitHub Desktop确认仓库和当前分支，先 Fetch/Pull。
3. 查看最近一次Actions运行，确认P0是否已解决。
4. 本地运行：

   ```bash
   python3 -m unittest discover -s tests -p "test_*.py"
   ```

5. 本地预览：

   ```bash
   python3 -m http.server 8000 --directory site
   ```

6. 浏览器检查地图、NOAA/CPC降水与气温图、ENSO/IOD区块、表格和来源说明。
7. 由维护者在GitHub Desktop执行 Commit、Push；发布仍由维护者在GitHub Actions操作。

## 七、主要文件

| 文件 | 用途 |
|---|---|
| `site/index.html` | 页面结构、图表和来源说明 |
| `site/assets/styles.css` | 页面及图表响应式样式 |
| `site/assets/app.js` | 地图、筛选、表格和详情交互 |
| `site/assets/climate/` | BoM生成图和NOAA RONI官方图 |
| `site/data/weather.json` | 当前天气与观测数据 |
| `site/data/history.json` | 历史预报快照 |
| `site/data/thailand-weekly-rain.json` | 泰国分区周度ERA5降雨序列 |
| `config/locations.json` | 地点、阈值和晨间割胶作业窗配置 |
| `scripts/update_weather.py` | 数据抓取、计算、验证和气候图更新 |
| `tests/test_update_weather.py` | 最小自动测试 |
| `.github/workflows/update-weather.yml` | 自动更新、提交和Pages部署 |

## 八、研究边界

- 天气、IMERG、ENSO、IOD和RONI均是 `CORE2_SUPPLY` 的背景或验证信息，不是天然橡胶供应事实本身。
- 不得将降雨、厄尔尼诺、拉尼娜或IOD状态机械解释为利多/利空。
- 后续判断仍需结合物候、开停割、原料供应、胶农利润、加工利润、进口、库存和价格结构交叉验证。
